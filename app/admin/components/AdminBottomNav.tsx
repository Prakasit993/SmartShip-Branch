'use client';

/**
 * เดิมเป็นเมนูล่างมือถือ — ปิดใช้งาน (คืน null) ใช้แฮมเบอร์เกอร์ + แถบด้านข้างแทน
 * คง export ไว้เพื่อให้ layout อ้างอิงได้สม่ำเสมอและหลีกเลี่ยง ReferenceError จากแคช / import หลุด
 */
export default function AdminBottomNav() {
    return null;
}
